# Отчет по ревью 
ФИО: Акименко Егор Сергеевич
Группа:35ис
Вариант:1
## 1.Что требует ТЗ:Возраст 18+ -> True младше 18 -> False
## 2.Что код делает на самом деле: Проверяет только age > 18. Если больше 18 - строка "да" Если младше 18 или меньше - ничего не возращает
## 3.Несоответствия: 
-Т3 "18 и больше" (>=18)
## 4.Вердикт: принять / принять с замечаниями / отклонить
## 5.Что исправить

Код на ревью
```py
def can_buy(age)
    if age > 18:
    return "да"
```

готовый код
```py
def can_buy(age):
    if age >= 18:
        return true
    return false
```

# Вариант 3
## 1. Что требует ТЗ:Если цена 1000 и больше - вернуть цену со скидкой 10%
## 2. Что код делает на самом деле: если цена больше 1000 то скидка 10%
## 3. Несоответсвие:
## 4. Вердикт: принять / принять с замечаниями / отклонить:
## 5. что исправить:
Код на ревью
```py
def final_price(price)
    if price > 1000
       return price - 10
```
Готовый код
```py
def final_price(price)
    if price > 1000
       return price - price * 0,1
    return price
```

# Вариант 4
## 1. Что требует ТЗ: баллы > 60 -зачет, если <60 -не зачет
## 2. Что код делает на самом деле: вводишь баллы если => 60 - зачет, если меньше то нету зачета
## 3. Несоответствия:
## 4. Вердикт: отклонить
## 5. Что исправить:
код на ревью
```py
def is_pass(score)
    if score >=60
       print("зачет")
```
готовый код
```py
def is_pass(score):
    if score >= 60:
        return True
    return False
```
# Варивант 5
## 1. Температура ниже 10 → нужна куртка (True), температура 10 и выше → куртка не нужна (False)
## 2. Что код делает на самом деле: При температуре ниже 10 возвращает False, при температуре 10 и выше ничего не возвращает
## 3. 3. В ТЗ при temp < 10 нужно возвращать True, в коде возвращается False, В ТЗ при temp >= 10 нужно возвращать False, в коде нет возврата вообще (возвращает None),Ошибка отступов после if temp < 10: — отсутствует отступ у return False
## 4. Вердикт: отклонить
## 5. Что исправить: Исправить логику: при temp < 10 возвращать True, при temp >= 10 возвращать False, Добавить правильные отступы, Добавить ветку else для случая temp >= 10

код на ревью
```py
def need_jacket(temp):
    if temp < 10:
        return false
```
готовый код
```py
def need_jacket(temp):
    if not isinstance(temp, (int, float)):
        return False
    if temp < 10:
        return True
    else:
        return False
